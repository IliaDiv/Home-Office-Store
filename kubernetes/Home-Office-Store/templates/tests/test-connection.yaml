apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Home-Office-Store.fullname" . }}-test-connection"
  labels:
    {{- include "Home-Office-Store.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Home-Office-Store.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
